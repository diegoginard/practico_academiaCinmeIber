# Slicing de listas

lista = ["lunes", "martes", "miércoles", "jueves", "viernes", "sábado", "domingo"]

print(len(lista))

""" print(f"A partir del índice 2 (incluido) hasta el índice 3 (excluido) {lista[2:3]}")

print(f"Desde el principio hasta el índice 3 (excluido) {lista[:3]}")

print(f"Desde el índice 3 (incluido) hasta el final {lista[3:]}")

print(f"Desde el principio hasta el final {lista[:]}")

print(
    f"Desde el índice 0 (incluido) hasta el índice 7 (excluido) con un paso de 2 {lista[0:7:2]}"
)

print(f"Desde el principio hasta el final con un paso de 2 {lista[::]}")

print(f"Desde el índice 1 (incluido) hasta el final con un paso de 2 {lista[1::2]}")

print(
    f"Desde el índice -1 (incluido) hasta el índice -7 (excluido) con un paso de -1 {lista[-1:-10:-1]}"
)

print(f"Desde el final hasta el principio con un paso de -1 {lista[::-1]}")

print(
    f"Desde el índice -1 (incluido) hasta el índice -10 (excluido) con un paso de -2 {lista[-1:-10:-2]}"
)

print(
    f"Desde el índice -1 (incluido) hasta el principio con un paso de -2 {lista[-1::-2]}"
)
 """
